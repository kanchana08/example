pipeline {
    agent any
    tools {nodejs "npm"}
    environment {
     //   snykTokenId = credentials('snyk-api-token') 
        SNYK_API = "${env.SNYK_API}"  
    }

    stages {
        stage('checkout') {
            steps {
                    
          git branch: 'master', credentialsId: 'PAT', url: 'https://git.stldev.io:1305/pradeepkumar.n/angular-ng.git'

                

            }
        }
        stage('Build') {
            steps {
                echo 'Building...'
                sh "npm install"
                sh "npm run build"

            }
        }
        stage('Test') {
      steps {
        echo 'Testing...'
        snykSecurity(
          snykInstallation: 'snyk@latest',
          snykTokenId: 'snyk-token',
          // place other parameters here
        )
      }
    }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Your deploy steps go here
            }
        }
    }
     post{ 

          success{ 

            mail to: "kanchanagm@stellaripl.com", 

             //cc: "nadimpali.pradeepk@stellaripl.com", 

            subject: "jenkins build:${currentBuild.currentResult}: ${env.JOB_NAME}", 

            body: "${currentBuild.currentResult}: Job ${env.JOB_NAME}\nMore Info can be found here: ${env.BUILD_URL}" 

             

        } 
}
}




pipeline {
    agent any
    tools { nodejs "npm" }
    environment {
        SNYK_API = "${env.SNYK_API}"  
    }

    stages {
        stage('checkout') {
            steps {
                git branch: 'master', credentialsId: 'PAT', url: 'https://git.stldev.io:1305/pradeepkumar.n/angular-ng.git'
            }
        }

        stage('Build') {
            steps {
                echo 'Building...'
                sh "npm install"
                sh "npm run build"
            }
        }

        stage('Test') {
            steps {
                echo 'Testing...'
                snykSecurity(
                    snykInstallation: 'snyk@latest',
                    snykTokenId: 'snyk-token',
                    // Ensure Snyk generates a report in HTML format
                    additionalArguments: '--report --report-file=snyk_report.html'
                )
            }
        }

        stage('Archive Report') {
            steps {
                echo 'Archiving Snyk Report...'
                archiveArtifacts artifacts: 'snyk_report.html', allowEmptyArchive: true
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Your deploy steps go here
            }
        }
    }

    post {
        success {
            echo 'Sending email with Snyk report...'
            mail to: "kanchanagm@stellaripl.com",
                 subject: "Jenkins Build: ${currentBuild.currentResult} - ${env.JOB_NAME}",
                 body: "${currentBuild.currentResult}: Job ${env.JOB_NAME}\nMore info: ${env.BUILD_URL}",
                 attachmentsPattern: 'snyk_report.html'
        }

        failure {
            mail to: "kanchanagm@stellaripl.com",
                 subject: "Jenkins Build Failed: ${env.JOB_NAME}",
                 body: "Build failed for job ${env.JOB_NAME}. Check the logs at: ${env.BUILD_URL}"
        }
    }
}


Testing...
[Pipeline] snykSecurity
Testing project...
> /var/lib/jenkins/tools/io.snyk.jenkins.tools.SnykInstallation/snyk_latest/snyk-linux test --json --severity-threshold=low --report --report-file=snyk_report.html
Generating report...
> /var/lib/jenkins/tools/io.snyk.jenkins.tools.SnykInstallation/snyk_latest/snyk-to-html-linux -i /var/lib/jenkins/workspace/snyk-email/2024-09-24T07-24-42-255405530Z_snyk_report.json
Archiving artifacts
Monitoring project...
> /var/lib/jenkins/tools/io.snyk.jenkins.tools.SnykInstallation/snyk_latest/snyk-linux monitor --severity-threshold=low --report --report-file=snyk_report.html

Monitoring /var/lib/jenkins/workspace/snyk-email (angular.io-example)...

Explore this snapshot at https://app.snyk.io/org/stellar_org/project/dff06cd3-c7d1-402d-909d-465d57fa5124/history/40830535-3074-4410-8eba-7cf96e8052de

Notifications about newly disclosed issues related to these dependencies will be emailed to you.

[Pipeline] }
[Pipeline] // withEnv
[Pipeline] }
[Pipeline] // stage
[Pipeline] stage
[Pipeline] { (Archive Report)
[Pipeline] tool
[Pipeline] envVarsForTool
[Pipeline] withEnv
[Pipeline] {
[Pipeline] echo
Archiving Snyk Report...
[Pipeline] archiveArtifacts
Archiving artifacts
‘snyk_report.html’ doesn’t match anything
No artifacts found that match the file pattern "snyk_report.html". Configuration error?
