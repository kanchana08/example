pipeline {
    agent any
    tools {nodejs "npm"}
    environment {
     //   snykTokenId = credentials('snyk-api-token') 
        SNYK_API = "${env.SNYK_API}"  
    }

    stages {
        stage('checkout') {
            steps {
                    
          git branch: 'master', credentialsId: 'PAT', url: 'https://git.stldev.io:1305/pradeepkumar.n/angular-ng.git'

                

            }
        }
        stage('Build') {
            steps {
                echo 'Building...'
                sh "npm install"
                sh "npm run build"

            }
        }
        stage('Test') {
      steps {
        echo 'Testing...'
        snykSecurity(
          snykInstallation: 'snyk@latest',
          snykTokenId: 'snyk-token',
          // place other parameters here
        )
      }
    }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Your deploy steps go here
            }
        }
    }
     post{ 

          success{ 

            mail to: "kanchanagm@stellaripl.com", 

             //cc: "nadimpali.pradeepk@stellaripl.com", 

            subject: "jenkins build:${currentBuild.currentResult}: ${env.JOB_NAME}", 

            body: "${currentBuild.currentResult}: Job ${env.JOB_NAME}\nMore Info can be found here: ${env.BUILD_URL}" 

             

        } 
}
}



Archiving artifacts
No artifacts found that match the file pattern "**/snyk_reports.html". Configuration error?

post { 
    success { 
        script {
            // List files in the workspace for debugging
            sh 'ls -R'
            
            // Archive the Snyk report
            archiveArtifacts artifacts: '**/snyk_reports.html', allowEmptyArchive: true
            
            // Send email with attachment
            emailext(
                to: "kanchanagm@stellaripl.com",
                subject: "Jenkins Build: ${currentBuild.currentResult} - ${env.JOB_NAME}", 
                body: "${currentBuild.currentResult}: Job ${env.JOB_NAME}\nMore Info can be found here: ${env.BUILD_URL}", 
                attachmentsPattern: "**/snyk_reports.html"
            )
        }
    } 
}




pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Your build steps here
            }
        }
        stage('Send Email') {
            steps {
                emailext(
                    subject: "Build #${currentBuild.number} - ${currentBuild.result}",
                    body: "Please find the build artifacts attached.",
                    to: 'recipient@example.com',
                    attachmentsPattern: '**/path/to/artifacts/*',
                    attachLog: true
                )
            }
        }
    }
    post {
        success {
            // Additional steps after a successful build
        }
        failure {
            // Additional steps after a failed build
        }
    }
}

